"use client";

import { createStyles } from "antd-style";

export const useStyles = createStyles(() => ({
  form: {
    maxHeight: "60vh",
    overflowY: "auto",
    paddingRight: 8,
  },
  fullWidth: {
    width: "100%",
  },
}));
